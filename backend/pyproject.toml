[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "faith-backend"
version = "0.0.0"
requires-python = ">=3.10"
description = "FAITH FastAPI backend (local app package)"

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]
